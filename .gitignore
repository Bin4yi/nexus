# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.dylib
*.dll
*.egg
*.egg-info/
dist/
build/
*.spec
MANIFEST
wheels/
sdist/
develop-eggs/
downloads/
parts/
var/

# ── Virtual environments ──────────────────────────────────────────────────────
.venv/
venv/
env/
ENV/
.env.bak
# Embedded venv created by `py -m ensurepip` in project root
Lib/
Scripts/
Include/
DLLs/
tcl/
pyvenv.cfg

# ── Environment / secrets ─────────────────────────────────────────────────────
# Keep .env.example but never commit the real .env (contains API keys)
.env
!.env.example
*.key
*.pem
secrets.yaml
secrets.json

# ── IDE / editor ──────────────────────────────────────────────────────────────
.vscode/
.idea/
*.code-workspace
*.suo
*.user
*.swp
*.swo
*~

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ── Test & coverage ───────────────────────────────────────────────────────────
.pytest_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
*.xml
junit*.xml
test-results/

# ── Type checkers & linters ───────────────────────────────────────────────────
.mypy_cache/
.ruff_cache/
.pytype/
.pyright/
pyrightconfig.json

# ── Logs & output ─────────────────────────────────────────────────────────────
*.log
*.log.*
output.txt
output_act.txt
logs/

# ── Jupyter ───────────────────────────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb

# ── Byte-compiled / type stubs ────────────────────────────────────────────────
*.pyi
py.typed

# ── Packaging / distribution ──────────────────────────────────────────────────
*.whl
*.tar.gz
*.zip

# ── Repository mirrors (large, re-cloneable on demand) ───────────────────────
mirror/
mirrors/

# ── Docker volumes (if bind-mounted locally) ──────────────────────────────────
neo4j_data/
neo4j_logs/
chroma_data/
chroma/
redis_data/
dump.rdb
volumes/

# ── RFC text cache (re-fetchable from rfc-editor.org) ────────────────────────
cache/
*.cache
data/rfcs/

# ── Write-ahead log (incremental updater) ────────────────────────────────────
wal.json

# ── ML model weights (large binaries, downloaded at runtime) ─────────────────
*.bin
*.safetensors
*.pt
*.pth
*.onnx
sentence_transformers/
models/
.cache/huggingface/
.cache/torch/

# ── Lock file (auto-generated, use requirements.txt instead) ─────────────────
requirements-lock.txt

# ── MyPy / Pyright caches ─────────────────────────────────────────────────────
.mypy_cache/
.nexus_watch_hashes.json
